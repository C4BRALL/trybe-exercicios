const stateContainer = document.getElementById('state');
function createOption (stateAbre, state) {
    let stateArray = []
    for (let i; i < stateArray.length; i+=1) {
        const optionE = document.createElement('option');
        optionE.value = stateAbre;
    }

}